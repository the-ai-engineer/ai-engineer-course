[project]
name = "project-1-terminal-agent"
version = "0.1.0"
description = "Project 1: Build an interactive terminal agent with tool calling"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0.0",
]

[tool.pyright]
typeCheckingMode = "basic"

